# Handoff — Redesign UI NSCMF dan analitik Dashboard (2026-09-24)

Branch: `feat/redesign` (lokal, belum di-push, belum ada PR). Basis: `c3170a9`. Rencana: [design.md](../design.md), [design/plan.md](../design/plan.md).

Catatan ini hanya mencatat status. `project_doc/` tetap otoritas.

## Keputusan pemilik (2026-09-24)

| Keputusan | Diterapkan di |
| --- | --- |
| Sinkronkan rencana ke dokumen resmi 01, 04, 07, 12, 17 | `7fcde2a`, gap G22 di `microtask_be/06_GAP_DAN_KEPUTUSAN.md` |
| Folder `desaign/` diganti menjadi `design/` | `7fcde2a` |
| Panel status memakai satu baris per status, bukan batang bersegmen. Alasannya, hijau dan merah gagal uji buta warna (deutan ΔE 1,1). | `892aa8b`, 07 §17.1 |
| Tiga file lama tidak disentuh: catatan audit di `handoff/2026-09-24-e2e-gap-closure.md`, `implementation.md`, `reference.webp` | Ketiganya tetap tidak di-commit |

Koreksi dokumen tanpa keputusan baru (`07f637d`): status CANCELLED hanya terjadi sebelum Submit (12 §17.1). Karena itu, status ini tidak mungkin ada di himpunan organisasi.

## Yang dikerjakan

**Backend** (RED `9cdde4f` → GREEN `3a5919d`)
- Izin baru `nscmf.analytics.view`, masuk bundle Superadmin saja. Seed referensi menambahkannya ke role yang sudah ada.
- `DashboardMetricsRepository` beserta implementasi Eloquent-nya menghitung per hari kalender Jakarta. `NscmfQueryService` menyusun prop `analytics`: periode 28 hari, 4 minggu, dan status aktif. Key `organization` hanya dikirim bila aktor punya izin analitik **dan** History.
- Tidak ada migrasi, endpoint, atau dependency baru.

**Frontend**
- Token visual (07 §7.1), utilitas `panel`, dan shell responsif (`e90e86d` → `8f4194c`):
  - sidebar putih dengan kelompok menu dan `aria-current`;
  - di layar sempit, sidebar menjadi panel dengan fokus terjaga dan bisa ditutup dengan Escape.
- Komponen bersama: `PageHeader`, `StatusBadge`, `BrandMark`, `CenteredLayout`, `controlClass`/`fileInputClass`, `Badge` nada info, serta `Modal`/`ActionDialog`/`ResourceTable` bergaya sama.
- Dashboard (`b295e6a` → `ec586a6`):
  - kartu adaptif 2/3/4 kolom, dengan kartu pertama sebagai sorotan;
  - panel Activity (warna sudah divalidasi, nilai mingguan juga tersedia sebagai tabel) dan Status;
  - pengalih Mine/Organization, Needs attention, dan Quick actions.
- Semua layar lain memakai sistem visual yang sama (`a28afa9`, `f70a527`).
- Waktu sign-off detail kini ditampilkan dalam waktu Jakarta (`3c47b6c` → `a8ec3c7`).
- Editor Draft kini punya navigator bagian (07 §21), yang sebelumnya belum ada (`7210334` → `6f23a77`).

## Bukti gate (dijalankan 2026-09-24 di commit `72e947c`)

| Gate | Hasil |
| --- | --- |
| Pint | PASS |
| PHPStan/Larastan max, tanpa baseline | PASS, 0 error |
| Pest + coverage (MySQL 8.4 `nscmf_testing`) | 507 lulus, cakupan baris 95,0 % |
| ESLint, Prettier, vue-tsc | PASS |
| Vitest + coverage | 729 lulus, cakupan baris 97,43 % |
| Vite build | PASS |
| Playwright Chromium, tanpa retry | 31/31 lulus |
| Test konfigurasi runtime browser | 5/5 lulus |

Pemeriksaan visual pada lebar 320, 768, dan 1280 px dilakukan lewat tangkapan layar di runtime browser sekali pakai. Tangkapan layar tidak disimpan di repo.

## Batasan yang perlu diketahui

- Test browser untuk analitik Dashboard dan panel navigasi ponsel (`72e947c`) ditulis **setelah** siklus RED/GREEN tingkat komponen. Test ini bukti integrasi, bukan RED pertama.
- Token `.dark` hanya dijaga kompatibel. Belum ada tombol mode gelap dan tampilan gelap belum diperiksa secara visual.
- Refactor visual murni memakai test yang sudah hijau sebagai pengaman, tanpa RED buatan.

## Perlu tindakan pemilik

1. Human implementation review (18 §29).
2. Human security review untuk izin baru `nscmf.analytics.view` dan agregat organisasi (18 §17).
3. Push branch dan buka PR. CI GitHub belum pernah berjalan untuk branch ini.
4. Putuskan nasib tiga file lama yang sengaja tidak disentuh.
